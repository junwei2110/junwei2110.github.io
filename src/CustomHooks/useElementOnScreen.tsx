import React from 'react';


const useElementOnScreen = () => {
    //return the observer 
    //create a observer using the intersectionObserver API with the right callback and options
    // observer needs to observe a ref
    // the ref needs to be attached to a node
    // 

}